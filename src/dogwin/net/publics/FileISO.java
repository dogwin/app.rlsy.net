package dogwin.net.publics;import java.io.File;import android.os.Environment;import android.util.Log;public class FileISO {	//write folder    public void writeFolder(File SDCardRoot){        if (!SDCardRoot.exists()) {            Log.d("DIRECTORY CHECK",                    "Directory doesnt exist creating directory "                            + Environment.getExternalStorageDirectory()                                    .toString());            boolean outcome = SDCardRoot.mkdirs();            Log.d("DIRECTORY CHECK",                    "outcome for " + SDCardRoot.getAbsolutePath() + "     "                            + outcome);        }else{        	//System.out.println("we found "+SDCardRoot);        }    }    //book exist	public Boolean bookExist(File bookName){    	if(bookName.exists()){    		return true;    	}else{    		return false;    	}    }}